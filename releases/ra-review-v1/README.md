# RA review v1

This stable review release updates the public presentation layer without
changing any experiment result or claim.

It provides:

- a current GitHub Pages entry that replaces the superseded 0.5B diagnostic
  landing page;
- explicit disclosure that the four RDS list realizations overlap
  substantially and are not four fully independent selection policies;
- a neutral research-at-a-glance section in both README files;
- a two-page English Research Snapshot generated from the canonical result
  JSON and frozen State Dependence v3 status;
- deindexing and canonical metadata for the retained historical HTML page;
- fail-fast Pages, snapshot, link, secret, result, and fresh-clone checks.

State Dependence v3 still has no GPU result. This release contains no new
training run and does not change the 24-cell statistics.
